import React from "react";
import { Link, graphql } from "gatsby";
import Layout from "../../components/layout";

function PhotoArea({ data }) {
  console.log(data);
  return <div>PhotoArea</div>;
}

export default PhotoArea;
